// Entry point. The full UI lands in plan step W8; until then this confirms the
// scaffold builds and serves.
const app = document.getElementById('app');
if (app) {
  app.textContent = 'AlphaBlokus web scaffold — UI coming in W8.';
}

export {};
