# Run artefacts

Large outputs referenced from `../box-results.md` — fetched HTML reports, ladder JSON, `.dat` files.
Keep the raw numbers in `box-results.md`; this is for the bulky evidence behind them.

Fetch a box report with `scripts/fetch_run_reports.sh`. Anything here is disposable — the box and
`temp/runs/` remain the source of truth.
